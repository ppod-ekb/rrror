create table user (
       id bigint not null,
        description varchar(255),
        first_name varchar(255),
        last_name varchar(255),
        login varchar(255),
        version bigint,
        primary key (id)
)